# -----------------------------
# 1. Dependencies (NEVER PUSH)
# -----------------------------
node_modules/
/backend/node_modules/
/frontend/node_modules/

# -----------------------------
# 2. Secrets (NEVER PUSH)
# -----------------------------
.env
/backend/.env
/frontend/.env
.env.local

# -----------------------------
# 3. Build Outputs (NEVER PUSH)
# -----------------------------
# Vercel will build this automatically, no need to push it
/frontend/dist/
/frontend/build/

# -----------------------------
# 4. Logs & OS Files
# -----------------------------
npm-debug.log*
.DS_Store